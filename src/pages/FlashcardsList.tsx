import React from 'react';
import 'react-toastify/dist/ReactToastify.css';

const FlashcardsList: React.FC = () => {
    return (
        <div>
            <h1>Gerar Flashcards</h1>
        </div>
    );
};

export default FlashcardsList;